# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# 1. เพิ่ม Memory ให้ Gradle (ป้องกันปัญหา OutOfMemoryError ตอน Build)
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# 2. เปิดใช้งาน Daemon (ทำให้รันครั้งต่อไปเร็วขึ้น เพราะไม่ต้องสตาร์ท Gradle ใหม่)
org.gradle.daemon=true

# 3. เปิดการ Build แบบขนาน (ใช้ CPU หลาย Core ช่วยกัน Build)
org.gradle.parallel=true

# 4. แคชผลลัพธ์การ Build (ประหยัดเวลามาก ถ้ารหัสส่วนนั้นไม่ได้ถูกแก้ไข)
org.gradle.caching=true

# 5. เปิดโหมด Configure on demand (ประมวลผลเฉพาะโมดูลที่เกี่ยวข้อง)
org.gradle.configureondemand=true

# 6. ตั้งค่าสำหรับ Flutter และ AndroidX (ต้องมีเสมอสำหรับโปรเจกต์ยุคใหม่)
android.useAndroidX=true
android.enableJetifier=true

# 7. ป้องกันปัญหาการแปลงโค้ด Kotlin
kotlin.code.style=official